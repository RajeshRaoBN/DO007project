---
- name: Basic System Setup
  hosts: all
  become: true
  tasks:
    - name: Update all security-related packages
      ansible.builtin.package:
        name: '*'
        state: "{{ package_state }}"
        security: "{{ security_only }}"
   
    - name: Create a new user
      ansible.builtin.user:
        name: "{{ item }}"
        state: present
        create_home: true
      loop:
        - alice
        - bob
        - charlie

    - name: Install Apache on the web server
      ansible.builtin.package:
        name: "{{ package_name }}"
        state: present
      when: inventory_hostname in group['web']

    - name: Install firewalld
      ansible.builtin.package:
        name: firewalld
        state: present
      when: inventory_hostname in group['web']

    - name: Ensure firewalld is running
      ansible.builtin.service:
        name: firewalld
        state: started
        enabled: true
      when: inventory_hostname in group['web']

    - name: Allow HTTP traffic on web server
      ansible.posix.firewalld:
        service: http
        permanent: true
        state: enabled
      when: inventory_hostname in group['web']
      notify: Reload firewall

  handlers:

    - name: Reload firewall
      ansible.builtin.service:
        name: firewalld
        state: reloaded